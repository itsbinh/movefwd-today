// Search components
export { SearchBar } from '../SearchBar'
export { SearchFiltersPanel } from '../SearchFilters'
export { ResourceSearch, useResourceSearch } from '../ResourceSearch'

// Types
export type { SearchFilters } from '@/types/search'
export {
  ELIGIBILITY_OPTIONS,
  US_STATES,
  DEFAULT_SEARCH_FILTERS,
  hasActiveFilters,
} from '@/types/search'
